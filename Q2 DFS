#include <iostream>
#include <vector>
using namespace std;

vector<vector<int>> g;
vector<int> vis;

void dfs(int node) {
    vis[node] = 1;
    cout << node << " ";

    for (int x : g[node]) {
        if (!vis[x]) dfs(x);
    }
}

int main() {
    int n, e;
    cin >> n >> e;
    g.resize(n);
    vis.assign(n, 0);

    for (int i = 0; i < e; i++) {
        int u, v;
        cin >> u >> v;
        g[u].push_back(v);
        g[v].push_back(u);
    }

    int start;
    cin >> start;

    dfs(start);
}
